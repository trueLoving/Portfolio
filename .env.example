# ============================================
# Environment Variables Configuration
# 环境变量配置
# ============================================
# Copy this file to .env and fill in your actual values
# 复制此文件到 .env 并填写实际值

# ============================================
# Required Variables (必需变量)
# ============================================

# Site Configuration
# 网站配置
# Your production site URL (used for SEO and Open Graph links)
# 生产环境网站 URL（用于 SEO 和 Open Graph 链接）
# Example: https://your-domain.tld or https://your-app.vercel.app
PUBLIC_SITE_URL=https://your-domain.tld

# AI Terminal Configuration
# AI 终端配置
# Groq API key for AI terminal chat functionality
# Groq API 密钥，用于 AI 终端聊天功能
# Get your API key from: https://console.groq.com/
GROQ_API_KEY=your_groq_api_key_here

# ============================================
# Optional Variables (可选变量)
# ============================================
# These are only needed if you want to use the contact form and admin dashboard
# 这些变量仅在需要使用联系表单和管理后台时需要配置

# Supabase Configuration (Server-only)
# Supabase 配置（仅服务器端使用）
# ⚠️ WARNING: Do NOT expose these in PUBLIC_ variables
# ⚠️ 警告：不要将这些变量暴露在 PUBLIC_ 前缀的变量中

# Your Supabase project URL
# Supabase 项目 URL
# Example: https://xxxxxxxxxxxxx.supabase.co
SUPABASE_URL=https://YOUR-PROJECT.supabase.co

# Supabase service role key (has full access, keep it secret!)
# Supabase 服务角色密钥（拥有完全访问权限，请保密！）
# Get this from: Supabase Dashboard → Settings → API → service_role key
# 获取方式：Supabase 控制台 → 设置 → API → service_role 密钥
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Admin Dashboard Credentials (Server-only)
# 管理后台凭证（仅服务器端使用）
# ⚠️ WARNING: Do NOT expose these in PUBLIC_ variables
# ⚠️ 警告：不要将这些变量暴露在 PUBLIC_ 前缀的变量中

# Admin username for accessing the admin dashboard
# 管理后台用户名
# Default: admin (change this to a secure username)
ADMIN_USERNAME=admin

# Admin password for accessing the admin dashboard
# 管理后台密码
# ⚠️ IMPORTANT: Change this to a strong password!
# ⚠️ 重要：请更改为强密码！
ADMIN_PASSWORD=change_me_to_strong_password

# ============================================
# Notes (注意事项)
# ============================================
# 1. Variables prefixed with PUBLIC_ are exposed to the client-side
#    以 PUBLIC_ 开头的变量会暴露到客户端
# 2. Server-only variables (without PUBLIC_ prefix) are only available on the server
#    仅服务器端变量（没有 PUBLIC_ 前缀）仅在服务器端可用
# 3. Never commit .env file to version control
#    永远不要将 .env 文件提交到版本控制系统
# 4. Make sure .env is in your .gitignore file
#    确保 .env 在 .gitignore 文件中
