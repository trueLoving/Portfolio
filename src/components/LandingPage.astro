---
import AppLayout from '../layouts/AppLayout';
import { getBackgroundMap, getRandomBackgroundKey } from '../config/background';

// 从配置文件获取背景资源
const backgroundMap = getBackgroundMap();
const initialBg = getRandomBackgroundKey();
---

<AppLayout client:load initialBg={initialBg} backgroundMap={backgroundMap} />
